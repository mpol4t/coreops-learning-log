# GitHub'a Ekleme

`day-17` klasörünü `coreops-learning-log` repo köküne kopyala.

```bash
cd ~/coreops-learning-log
git status
git pull --rebase origin main

git add day-17
git status
git diff --cached

git commit -m "Add Day 17 exception and process boundary notes"
git push origin main
git status
```

`VALIDATION.md` yalnızca bu hazırlama paketinin kontrol raporudur; repoya eklemek zorunda değilsin.
